import NotFound from '@/pages/_components/NotFound';

// export const metadata: Metadata = {
//   title: 'ページが見つかりません',
// };

const Page = () => {
  return (
    <main style={{ height: '100vh' }}>
      <NotFound />
    </main>
  );
};

export default Page;
